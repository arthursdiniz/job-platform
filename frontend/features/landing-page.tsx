'use client';

import { Link, useNavigate } from 'react-router-dom';
import { ArrowRight, BarChart3, BriefcaseBusiness, CheckCircle2, Code2, MapPin, Search, ShieldCheck, Sparkles, Users } from 'lucide-react';
import { useState } from 'react';
import { useJobs } from '@/lib/queries';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Badge } from '@/components/ui/badge';
import { Card, CardContent } from '@/components/ui/card';
import { BlurFade } from '@/components/ui/blur-fade';
import { ShineBorder } from '@/components/ui/shine-border';
import { JobCard, LoadingCards } from '@/components/app/common';

const steps = [{ icon: Users, title: 'Monte seu perfil', text: 'Registre competências, experiência e o tipo de oportunidade que procura.' }, { icon: Sparkles, title: 'Descubra afinidade', text: 'Veja uma pontuação transparente baseada nas habilidades da vaga.' }, { icon: BarChart3, title: 'Acompanhe tudo', text: 'Candidaturas e processos seletivos organizados em uma única visão.' }];
const categories = ['Desenvolvimento', 'Dados & IA', 'Produto', 'Design', 'Cloud & DevOps', 'Segurança'];

export function LandingPage() {
  const navigate = useNavigate();
  const [title, setTitle] = useState(''); const [city, setCity] = useState('');
  const jobs = useJobs({ size: 3 });
  const search = (event: React.SyntheticEvent<HTMLFormElement>) => { event.preventDefault(); const q = new URLSearchParams(); if (title) q.set('title', title); if (city) q.set('city', city); void navigate(`/jobs?${q}`); };
  return <>
    <section className="relative overflow-hidden border-b"><div className="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_72%_20%,var(--surface-tint),transparent_36%)]" /><div className="mx-auto grid max-w-7xl gap-12 px-6 py-20 lg:grid-cols-[1.1fr_.9fr] lg:py-28"><BlurFade><Badge variant="outline" className="mb-6 border-primary/25 bg-primary/5 text-primary"><Sparkles /> Afinidade além das palavras-chave</Badge><h1 className="max-w-3xl text-5xl font-semibold leading-[1.03] tracking-[-.045em] sm:text-6xl lg:text-7xl">Encontre o trabalho que leva você ao próximo <span className="text-primary">vértice.</span></h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted-foreground">Vagas em tecnologia, recomendações claras e uma experiência feita para aproximar talentos e empresas.</p><form onSubmit={search} className="mt-9 grid max-w-3xl gap-3 rounded-xl border bg-card p-3 shadow-xl sm:grid-cols-[1fr_1fr_auto]"><label htmlFor="home-title" className="flex items-center gap-2 px-2"><Search className="size-4 text-muted-foreground" /><Input id="home-title" aria-label="Cargo ou tecnologia" value={title} onChange={e => setTitle(e.target.value)} placeholder="Cargo ou tecnologia" className="border-0 bg-transparent shadow-none focus-visible:ring-0" /></label><label htmlFor="home-city" className="flex items-center gap-2 border-t px-2 sm:border-l sm:border-t-0"><MapPin className="size-4 text-muted-foreground" /><Input id="home-city" aria-label="Cidade" value={city} onChange={e => setCity(e.target.value)} placeholder="Cidade" className="border-0 bg-transparent shadow-none focus-visible:ring-0" /></label><Button size="lg" type="submit">Buscar vagas</Button></form><div className="mt-5 flex flex-wrap items-center gap-4 text-sm text-muted-foreground"><span className="flex items-center gap-1.5"><CheckCircle2 className="size-4 text-primary" /> Cadastro gratuito</span><span className="flex items-center gap-1.5"><ShieldCheck className="size-4 text-primary" /> Seus dados sob controle</span></div></BlurFade><BlurFade delay={.15} className="relative hidden min-h-[440px] lg:block"><div className="absolute inset-8 rotate-2 rounded-2xl bg-primary/8" /><Card className="absolute inset-0 overflow-hidden border-primary/20 bg-card/90 p-3 shadow-2xl backdrop-blur"><ShineBorder shineColor={['#0f766e', '#f59e0b']} duration={10} /><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-widest text-muted-foreground">Seu match desta semana</p><p className="mt-2 text-2xl font-semibold">Engenheiro(a) Backend</p></div><span className="grid size-16 place-items-center rounded-full border-4 border-primary/25 text-lg font-bold text-primary">92%</span></div><div className="mt-8 space-y-3">{['Java & Spring Boot', 'PostgreSQL', 'APIs REST', 'Docker'].map((skill, index) => <div key={skill} className="flex items-center justify-between rounded-lg bg-secondary/70 px-4 py-3"><span className="flex items-center gap-2"><Code2 className="size-4 text-primary" />{skill}</span><Badge variant={index < 3 ? 'default' : 'outline'}>{index < 3 ? 'Compatível' : 'Desejável'}</Badge></div>)}</div><div className="mt-8 rounded-xl bg-primary p-5 text-primary-foreground"><p className="text-sm opacity-75">Por que combina com você</p><p className="mt-2 leading-7">Seu perfil atende 3 das 4 competências principais e está no nível de experiência esperado.</p></div></CardContent></Card></BlurFade></div></section>
    <section id="como-funciona" className="mx-auto max-w-7xl px-6 py-20"><BlurFade inView><p className="text-xs font-semibold uppercase tracking-[.2em] text-primary">Uma jornada mais clara</p><h2 className="mt-3 max-w-2xl text-4xl font-semibold tracking-tight">Menos ruído. Mais oportunidades relevantes.</h2></BlurFade><div className="mt-10 grid gap-5 md:grid-cols-3">{steps.map(({ icon: Icon, title: stepTitle, text }, index) => <BlurFade key={stepTitle} inView delay={index * .08}><Card className="h-full"><CardContent className="p-6"><span className="grid size-11 place-items-center rounded-lg bg-primary/10 text-primary"><Icon /></span><p className="mt-5 text-lg font-semibold">{stepTitle}</p><p className="mt-2 leading-7 text-muted-foreground">{text}</p></CardContent></Card></BlurFade>)}</div></section>
    <section className="border-y bg-secondary/35"><div className="mx-auto max-w-7xl px-6 py-20"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-primary">Agora na plataforma</p><h2 className="mt-3 text-4xl font-semibold tracking-tight">Vagas recém-publicadas</h2></div><Button variant="outline" nativeButton={false} render={<Link to="/jobs" />}>Ver todas <ArrowRight /></Button></div><div className="mt-9">{jobs.isLoading ? <LoadingCards /> : jobs.data?.content.length ? <div className="grid gap-4">{jobs.data.content.map(job => <JobCard key={job.id} job={job} />)}</div> : <p className="rounded-xl border bg-card p-8 text-muted-foreground">Nenhuma vaga aberta no momento.</p>}</div></div></section>
    <section className="mx-auto max-w-7xl px-6 py-20"><div className="grid gap-8 lg:grid-cols-[1fr_1.2fr]"><div><BriefcaseBusiness className="size-8 text-primary" /><h2 className="mt-5 text-4xl font-semibold tracking-tight">Explore por especialidade</h2><p className="mt-3 max-w-lg leading-7 text-muted-foreground">Comece por uma área e refine por experiência, localidade e modelo de trabalho.</p></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-3">{categories.map(category => <Link key={category} to={`/jobs?skill=${encodeURIComponent(category)}`} className="rounded-xl border bg-card p-5 font-medium transition hover:border-primary/40 hover:text-primary">{category}<ArrowRight className="mt-8 size-4" /></Link>)}</div></div></section>
    <section className="mx-auto max-w-7xl px-6 pb-20"><div className="relative overflow-hidden rounded-2xl bg-[#082f3a] px-7 py-12 text-white sm:px-12"><div className="absolute -right-20 -top-28 size-80 rounded-full border border-white/10" /><p className="text-sm font-medium text-amber-300">Pronto para o próximo passo?</p><h2 className="mt-3 max-w-2xl text-4xl font-semibold tracking-tight">Seu próximo trabalho pode começar por uma conversa melhor.</h2><div className="mt-7 flex flex-wrap gap-3"><Button className="bg-amber-400 text-slate-950 hover:bg-amber-300" nativeButton={false} render={<Link to="/register" />}>Criar perfil gratuito <ArrowRight /></Button><Button variant="outline" className="border-white/25 bg-transparent text-white hover:bg-white/10" nativeButton={false} render={<Link to="/jobs" />}>Explorar vagas</Button></div></div></section>
  </>;
}
